client:
  server_endpoint: "localhost:46219"
  instance_description:
    service_name: reporter
    instance_name: test
  periodicity: 1s
  verbose: false

scenario:
  steps:
    # linear growth, 1 Kb / sec
    - memory_delta: 1024
      wait: 1s
